import{_ as s,o as a,c as t,a2 as i}from"./chunks/framework.mRCPFc5l.js";const k=JSON.parse('{"title":"02 · 工具系统与执行管道","description":"","frontmatter":{},"headers":[],"relativePath":"deep-dive/tools.md","filePath":"deep-dive/tools.md","lastUpdated":null}'),o={name:"deep-dive/tools.md"};function n(l,e,d,c,r,p){return a(),t("div",null,[...e[0]||(e[0]=[i(`<h1 id="_02-·-工具系统与执行管道" tabindex="-1">02 · 工具系统与执行管道 <a class="header-anchor" href="#_02-·-工具系统与执行管道" aria-label="Permalink to &quot;02 · 工具系统与执行管道&quot;">​</a></h1><blockquote><p>一次工具调用在 DSH 里要穿过一条九段流水线：materialize → pre-execute → guard → approval → execute(around) → 函数体 → post-execute → finalizeContent → result。每一段都是策略插件的挂载点。本章基于 <code>packages/core/tools/</code>（约 5,200 行）与 <code>packages/core/agent-loop/src/tool-calls.ts</code> 的真实源码。</p></blockquote><h2 id="_1-概念地图" tabindex="-1">1. 概念地图 <a class="header-anchor" href="#_1-概念地图" aria-label="Permalink to &quot;1. 概念地图&quot;">​</a></h2><p>DSH 工具系统三层：</p><ol><li><strong>注册层</strong>：<code>ctx.tools</code>（<code>ToolRuntime</code>）按 agent scope 分层注册/遮蔽/限制工具，并把面向模型的 schema 投影进 system prompt</li><li><strong>执行层</strong>：pre-execute → guard → execute → post-execute → finalize → result 流水线，策略以 waterfall 事件挂载</li><li><strong>能力层</strong>：工具本体不碰操作系统，消费抽象 seam（<code>ctx.shell</code>、<code>ctx.fs</code>、<code>ctx.subprocess</code>、<code>ctx.sandbox</code>）</li></ol><p>核心术语：</p><table tabindex="0"><thead><tr><th>术语</th><th>含义</th></tr></thead><tbody><tr><td><code>ToolDefinition</code></td><td>已注册工具：schema + output 契约 + <code>execute</code> + 可选 <code>finalizeContent</code>/<code>presentCall</code>/<code>presentResult</code>/<code>timeoutMs</code>/<code>isConcurrencySafe</code></td></tr><tr><td><code>executionMode</code></td><td><code>parallel</code>（可与兄弟重叠）/ <code>exclusive</code>（独占屏障）</td></tr><tr><td><code>ToolGuard</code></td><td>单调守卫：只能拒绝，<strong>没有 allow 分支</strong></td></tr><tr><td><code>ToolExecutionResult</code></td><td>结果闭集：Success <code>{isError:false, value, content, ...}</code> / Failure <code>{isError:true, error, content}</code></td></tr><tr><td>canonical value</td><td>工具函数体的返回值，<strong>只活在执行期</strong>；持久化只留 <code>content/error/meta</code></td></tr><tr><td>Code Mode</td><td><code>run_code</code> 保留传输：模型写程序，程序内工具调用重入完整流水线</td></tr></tbody></table><h2 id="_2-关键类型-tooldefinition" tabindex="-1">2. 关键类型：ToolDefinition <a class="header-anchor" href="#_2-关键类型-tooldefinition" aria-label="Permalink to &quot;2. 关键类型：ToolDefinition&quot;">​</a></h2><div class="language-ts vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// packages/core/tools/src/index.ts:222（研究笔记摘录）</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">interface</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ToolDefinition</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> extends</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ToolSchema</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  readonly</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> output</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ToolOutputDefinition</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">          // 必填：schema + render + presentationMeta</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  execute</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">args</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> unknown</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">exec</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ToolRunContext</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Promise</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">unknown</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  finalizeContent</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">exec</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">result</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ContentBlock</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[] </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">|</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> undefined</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  timeoutMs</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> number</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                              // 合作式超时预算，绝不下发给模型</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  isConcurrencySafe</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">args</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> unknown</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> boolean</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">      // 纯同步分类器，只有精确 true 才并行</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  presentCall</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">args</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ToolCallView</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> undefined</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // UI 卡片的调用视图</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  presentResult</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">args</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">result</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ToolResultView</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> undefined</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br></div></div><p>四个关键设计：</p><ol><li><strong><code>execute</code> 返回 canonical value，<code>output.render</code> 投影成模型可见的 <code>ContentBlock[]</code></strong>——value 可无损 JSON 快照校验，渲染与执行解耦</li><li><strong>schema 白名单投影</strong>：<code>schemaOf()</code> 只把 <code>{name, description, parameters}</code> 给模型；<code>timeoutMs</code>/<code>isConcurrencySafe</code> 是宿主元数据，永不泄漏（<code>index.ts:1256</code>）</li><li><strong><code>finalizeContent</code> 在执行开始时就被快照</strong>（WeakMap），对每个归一化结果恰好调用一次，只能改 content</li><li><strong><code>isConcurrencySafe</code> 是&quot;精确 true 才并行&quot;</strong>：未知/隐藏/抛异常一律 exclusive（fail-closed）</li></ol><h2 id="_3-完整执行流水线-execute-源码走读" tabindex="-1">3. 完整执行流水线（execute 源码走读） <a class="header-anchor" href="#_3-完整执行流水线-execute-源码走读" aria-label="Permalink to &quot;3. 完整执行流水线（execute 源码走读）&quot;">​</a></h2><p><code>ToolRuntime.execute(exec)</code> → <code>prepareExecution</code> → <code>completeScheduledExecution</code>，五阶段：</p><h3 id="阶段-a-materialize-策略之前" tabindex="-1">阶段 A：materialize（策略之前） <a class="header-anchor" href="#阶段-a-materialize-策略之前" aria-label="Permalink to &quot;阶段 A：materialize（策略之前）&quot;">​</a></h3><ul><li>分配 token、快照+冻结 arguments（<code>snapshotJsonValue</code> + <code>deepFreeze</code>）</li><li><strong>collapse 判定</strong>：Code Mode 下模型直接调非 <code>run_code</code> → 在策略流水线<strong>之前</strong>确定性拒绝——pre-execute/approval/guard 永远看不到它</li><li>已取消 → <code>ABORTED_BEFORE_DISPATCH</code></li></ul><h3 id="阶段-b-pre-execute-guard-策略前门" tabindex="-1">阶段 B：pre-execute + guard（策略前门） <a class="header-anchor" href="#阶段-b-pre-execute-guard-策略前门" aria-label="Permalink to &quot;阶段 B：pre-execute + guard（策略前门）&quot;">​</a></h3><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>1. ctx.waterfall(&#39;tools/pre-execute&#39;, exec, () =&gt; allow)   ← 钩子/审批策略挂这里</span></span>
<span class="line"><span>2. ask → ctx.approval.request()                            ← 无服务/无 agent → deny</span></span>
<span class="line"><span>3. guard 链：每个守卫返回 reason 即拒绝，第一个生效       ← 只能减权限</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><h3 id="阶段-c-around-dispatch-函数体" tabindex="-1">阶段 C：around dispatch + 函数体 <a class="header-anchor" href="#阶段-c-around-dispatch-函数体" aria-label="Permalink to &quot;阶段 C：around dispatch + 函数体&quot;">​</a></h3><ul><li><code>ctx.waterfall(&#39;tools/execute&#39;, mutableExec, () =&gt; dispatchToolBody)</code>——超时策略（<code>packages/guard/timeout-policy</code>）就是这里的包装器：读 <code>timeoutMs</code> → 造 deadline → <strong>临时替换 <code>exec.signal</code></strong> → <code>next()</code></li><li>注册表用 <code>fuseToolSignals(callerSignal, wrapperSignal)</code> 把两者融合再进函数体：<strong>包装层无法脱离调用方取消</strong></li><li>函数体期间 <code>deferContext()</code> 收集的上下文合并进结果的 <code>additionalContexts</code></li></ul><h3 id="阶段-d-post-execute-finalize" tabindex="-1">阶段 D：post-execute + finalize <a class="header-anchor" href="#阶段-d-post-execute-finalize" aria-label="Permalink to &quot;阶段 D：post-execute + finalize&quot;">​</a></h3><ul><li><code>tools/post-execute</code> waterfall：<code>accept</code> / <code>accept+value</code>（重校验重渲染）/ <code>accept+content</code>（换内容保 value）/ <code>block</code>（变 isError，feedback 即 content）；<strong>value 与 content 不能同时替换</strong>（显式抛 TypeError）</li><li><code>materializeFinalResult</code>（lossless 快照 + freeze，失败→isError）→ <code>applyFinalContent</code>（快照的 finalizeContent）→ 再 materialize</li></ul><h3 id="阶段-e-tools-result-冻结的权威结果" tabindex="-1">阶段 E：tools/result（冻结的权威结果） <a class="header-anchor" href="#阶段-e-tools-result-冻结的权威结果" aria-label="Permalink to &quot;阶段 E：tools/result（冻结的权威结果）&quot;">​</a></h3><ul><li>先 <code>Object.freeze(exec)</code>，再同步 emit <code>tools/result</code>；<strong>观察者异常被隔离，不能变换结果</strong></li></ul><div class="tip custom-block"><p class="custom-block-title">面试要点：为什么&quot;value 只活在执行期&quot;？</p><p>持久化只留 <code>content/error/meta</code>。审计日志与回放依赖的是模型可见的内容（content），canonical value 是内部协议。把 value 持久化会导致&quot;模型看到的&quot;与&quot;日志记录的&quot;出现第二真源。这是&quot;模型可见 ⟺ 已记录&quot;不变量在工具层的投影。</p></div><h2 id="_4-双轨策略-waterfall-单调守卫" tabindex="-1">4. 双轨策略：waterfall + 单调守卫 <a class="header-anchor" href="#_4-双轨策略-waterfall-单调守卫" aria-label="Permalink to &quot;4. 双轨策略：waterfall + 单调守卫&quot;">​</a></h2><p>DSH 把&quot;可扩展&quot;与&quot;不可撤销&quot;分得非常清楚（<code>docs/subsystems/tools.zh.md:313</code>）：</p><table tabindex="0"><thead><tr><th></th><th><code>tools/pre-execute</code> waterfall</th><th><code>ToolGuard</code></th></tr></thead><tbody><tr><td>决策</td><td>allow / deny / <strong>ask</strong></td><td>只能返回拒绝 reason</td></tr><tr><td>顺序</td><td>敏感（监听器顺序影响结果）</td><td>无关（谁先拒绝都一样）</td></tr><tr><td>可组合</td><td>是（可多层包裹）</td><td>否（扁平链）</td></tr><tr><td>用途</td><td>钩子、审批策略</td><td><strong>owner 策略</strong>（沙箱模式检查等不可绕过的规则）</td></tr></tbody></table><p>为什么需要两套？因为 waterfall 的顺序可以把一个拒绝&quot;翻案&quot;成放行（外层监听器先返回 allow 就短路了）。守卫没有 allow 分支，<strong>注册后顺序无法把拒绝翻回放行</strong>。</p><h2 id="_5-schema-dsl-类型安全的工具定义" tabindex="-1">5. schema DSL：类型安全的工具定义 <a class="header-anchor" href="#_5-schema-dsl-类型安全的工具定义" aria-label="Permalink to &quot;5. schema DSL：类型安全的工具定义&quot;">​</a></h2><p><code>defineTool()</code>（<code>packages/core/tools/src/schema.ts:545</code>）是 DSH 最精巧的 API 之一：</p><div class="language-ts vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">defineTool</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  name: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;bash&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  parameters: {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    command: StringSchemaSpec.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">required</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(),        </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 必填</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    timeoutMs: IntegerValueSchemaSpec,           </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 可选</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    workdir: StringValueSchemaSpec,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  output: { schema: </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">...</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">render</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">value</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ContentBlock[] },</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  execute</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">async</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">args</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">exec</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">...</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },        </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// args 类型由 parameters 推导！</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">})</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br></div></div><ul><li><code>InferValue&lt;S&gt;</code> 精确推导到 <strong>16 层容器</strong>后回退 <code>JsonValue</code>，避免类型实例化栈耗尽</li><li>编译是<strong>栈安全的任务图</strong>（防深递归/循环 schema）</li><li>原始 JSON Schema 只支持 <code>type/oneOf/properties/required/additionalProperties/items/enum/const</code> 子集；<strong>不支持的关键字直接拒绝，不静默放行</strong></li></ul><div class="tip custom-block"><p class="custom-block-title">面试要点：schema 校验发生在哪些边界？</p><p>DSH 的校验哲学（根 AGENTS.md）：<strong>信任 TypeScript 的同进程边界，在解析/配置/模型输出/持久化/worker/进程/网络边界做运行时校验</strong>。工具参数来自模型 = 不可信边界，所以有完整的 schema 编译+校验+<code>INVALID_ARGS</code> 结构化错误。</p></div><h2 id="_6-调度器回顾-与第-01-章联动" tabindex="-1">6. 调度器回顾（与第 01 章联动） <a class="header-anchor" href="#_6-调度器回顾-与第-01-章联动" aria-label="Permalink to &quot;6. 调度器回顾（与第 01 章联动）&quot;">​</a></h2><p>上一章的 <code>executeToolCalls</code> 细节在此补全（<code>packages/core/agent-loop/src/tool-calls.ts</code>）：</p><ul><li><code>executionMode()</code> 分类：只有 <code>isConcurrencySafe(args) === true</code> 才 parallel</li><li><strong>reclassify</strong>：<code>fillPool</code> 每次启动前重读后续调用 mode——注册表/守卫在提交过程中可能变化，后调用变 exclusive 则当前池排空后停在屏障</li><li>exclusive = 顺序屏障；parallel = 滚动池（容量 <code>maxParallelToolCalls</code>）</li><li>模型序提交（<code>commitReady</code> 只推进连续前缀槽位）</li></ul><p><strong>为什么这样设计</strong>：策略（pre/guard）必须保序且可等待；函数体可以重叠（LLM 一次发多个并行调用）；reclassify 让策略变更即时生效而不是僵化地按第一眼分类。</p><h2 id="_7-取消语义的分层" tabindex="-1">7. 取消语义的分层 <a class="header-anchor" href="#_7-取消语义的分层" aria-label="Permalink to &quot;7. 取消语义的分层&quot;">​</a></h2><table tabindex="0"><thead><tr><th>场景</th><th>结果</th></tr></thead><tbody><tr><td>body 未启动就取消</td><td><code>ABORTED_BEFORE_DISPATCH</code></td></tr><tr><td>body 已启动后取消</td><td>排空到 quiescence 后，成功结果被 <code>ABORTED</code> 替换</td></tr><tr><td>已启动的失败</td><td>保留工具自有结构化错误（<code>ToolFailure.info</code> 带 <code>{name, code}</code>）</td></tr><tr><td>调度器自身失败</td><td>停止新分发、排空已启动、<strong>不伪造结果</strong>、reject 首个失败</td></tr></tbody></table><p>关键承认（写在 JSDoc 里）：<strong>注册表无法硬杀同进程代码</strong>。超时只能靠工具合作转发 <code>exec.signal</code>。这也是文件系统 IO 刻意不设超时的原因——本地 <code>fsync</code>/<code>rename</code> 无法被强制中止，超时会是 seam 无法执行的空头支票。</p><h2 id="_8-工具目录速览" tabindex="-1">8. 工具目录速览 <a class="header-anchor" href="#_8-工具目录速览" aria-label="Permalink to &quot;8. 工具目录速览&quot;">​</a></h2><p>按 Consumer 归族（完整目录见 <code>docs/tool-catalog.md</code>）：</p><table tabindex="0"><thead><tr><th>族</th><th>工具</th><th>背后 seam</th></tr></thead><tbody><tr><td>执行</td><td><code>bash</code>、<code>pwsh</code>、<code>run_code</code></td><td><code>ctx.shell</code> / <code>ctx.codeRuntime</code></td></tr><tr><td>文件</td><td><code>read</code>/<code>write</code>/<code>edit</code>/<code>read_image</code>、<code>glob</code>/<code>grep</code></td><td><code>ctx.fs</code> / <code>ctx.subprocess</code></td></tr><tr><td>委派</td><td><code>subagent</code>/<code>subagent_fork</code>/<code>interrupt_agent</code>/<code>list_agents</code>/<code>send_message</code>、<code>workflow</code>、<code>ralph</code></td><td><code>ctx.subagents</code> / <code>ctx.workflowEngine</code></td></tr><tr><td>交互</td><td><code>ask_user_question</code>、<code>exit_plan_mode</code>、<code>create_goal</code>/<code>get_goal</code>/<code>update_goal</code></td><td><code>ctx.userQuestions</code> / <code>ctx.goals</code></td></tr><tr><td>状态</td><td><code>todo_write</code>、<code>job_kill</code>/<code>job_list</code>/<code>job_output</code>、<code>terminal_*</code></td><td><code>ctx.jobs</code> / <code>ctx.terminals</code></td></tr><tr><td>检索</td><td><code>web_search</code>/<code>web_fetch</code>、<code>lsp</code>、<code>session_*</code>、<code>skill</code></td><td><code>ctx.web</code> / <code>ctx.lsp</code></td></tr><tr><td>外部</td><td><code>mcp__&lt;server&gt;__&lt;tool&gt;</code></td><td>MCP 桥接</td></tr></tbody></table><h2 id="_9-mcp-桥接-外部工具的-一等公民化" tabindex="-1">9. MCP 桥接：外部工具的&quot;一等公民化&quot; <a class="header-anchor" href="#_9-mcp-桥接-外部工具的-一等公民化" aria-label="Permalink to &quot;9. MCP 桥接：外部工具的&quot;一等公民化&quot;&quot;">​</a></h2><p><code>packages/mcp/mcp-client</code>：每实例连一个 MCP server（stdio / streamable-http），<code>tools/list</code> 发现 → 生成 <code>ToolDefinition</code>（公共名 <code>mcp__&lt;server&gt;__&lt;raw&gt;</code>，超长/非法字符附 SHA-256 哈希去碰撞）→ 原子换代注册。<strong>MCP 工具走完整流水线</strong>——同样可以被审批、沙箱、守卫管到。这就是&quot;一切皆插件&quot;的威力：外部协议工具与本机工具在策略层面完全平等。</p><h2 id="_10-面试要点汇总" tabindex="-1">10. 面试要点汇总 <a class="header-anchor" href="#_10-面试要点汇总" aria-label="Permalink to &quot;10. 面试要点汇总&quot;">​</a></h2><div class="tip custom-block"><p class="custom-block-title">面试要点 1：pre-execute 与 guard 的区别？</p><p>瀑布可 allow/deny/ask 且可组合；守卫只能拒绝、顺序无法翻案。owner 策略放守卫。</p></div><div class="tip custom-block"><p class="custom-block-title">面试要点 2：如何做到 per-agent 工具隔离？</p><p><code>ScopedLayers</code>（global + 每 scope 层）+ restriction（<code>allow</code>/<code>deny</code>）只过滤<strong>继承面</strong>（全局+祖先），<strong>本 scope 自己的注册不受限</strong>——子 agent 保留回报工具靠的就是这个。</p></div><div class="tip custom-block"><p class="custom-block-title">面试要点 3：给所有工具加审计怎么做？</p><p>监听 <code>tools/pre-execute</code> 或 <code>tools/result</code>（scope 过滤按 agent），<strong>不动 agent-loop</strong>。</p></div><div class="tip custom-block"><p class="custom-block-title">面试要点 4：为什么工具结果要&quot;freeze 后同步通知&quot;？</p><p><code>tools/result</code> 观察者拿到的是冻结的权威结果，异常被隔离。观察者不能改写结果 = 审计与回放确定性。任何想改结果的策略必须回到 post-execute。</p></div><p>下一篇：<a href="/deep-dive/sandbox">03 · 沙箱与权限</a>，看 Landlock 自限制 launcher 如何用 300 行 C 实现无 root 进程沙箱。</p>`,51)])])}const u=s(o,[["render",n]]);export{k as __pageData,u as default};
